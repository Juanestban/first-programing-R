# vectors
# vectors is declare width c()
vector = c('Shrek', 'Shrek 2', 'Shrek Tercero', 'Shrek Felices por siempre')
score = c(7.9, 7.2, 6.1, 6.3)
nextYear = c(FALSE, FALSE, TRUE, TRUE)

# mean() get the promed or media || length()
# nextYear[1]

vector[c(1,4)]

# Matrices
